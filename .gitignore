# Dependencies — restored with `npm install`.
node_modules/

# Build output. Everything under out/ is regenerated by tools/ from the
# artwork and audio that *are* tracked: renders, the mixed teaser, the book,
# the generated music and the TTS takes. Around 120 MB, none of it a source.
out/

# A colleague's reference video — theirs, not ours, and 24 MB.
assets/ref/

# macOS / editor litter
.DS_Store
__pycache__/
*.pyc
.venv/
venv/

# Assembled by tools/site.mjs for deployment.
dist/

# The delivered film master, 412 MB. tools/video.mjs encodes it to out/film/
# and the site ships only that; GitHub blocks files this size anyway, and it
# would sit in the history of a public repo forever.
assets/incoming/فيلم/
